# 上海地铁票价查询 API 文档

> 本文档记录本项目的全部接口，包含请求地址、请求方法、参数、返回结果示例及字段说明。

---

## 基本信息

| 项目 | 说明 |
|------|------|
| 服务地址 | `http://localhost:3000`（本项目自己的 Node 服务） |
| 启动命令 | `npm start` |
| 数据来源 | **上海地铁官网** `service.shmetro.com`（由后端抓取） |
| 前端页面 | `http://localhost:3000/` |

### 数据流向（重要）

```
用户浏览器 (localhost:3000 前端页面)
        │ ① 浏览器只请求你自己的后端，不直接访问官网
        ▼
Node 后端 (localhost:3000/api/...)
        │ ② 后端从这里抓取真实票价
        ▼
上海地铁官网 (service.shmetro.com/i/p)
```

> **说明**：`API.md` 里写的所有 `localhost:3000` 都是**你自己后端的地址**，不是数据源头。
> 真实票价是 Node 后端调用上海地铁官网 `https://service.shmetro.com/i/p` 拿到的，因为浏览器直接请求官网会因跨域(CORS)被拦截，所以由你的后端做中转代理。

---

## 一、站点列表接口

### `GET /api/stations`

返回地铁网络全部站点的代码和名称。

**请求示例：**
```
GET http://localhost:3000/api/stations
```

**返回结果（截取前几条）：**
```json
[
  { "code": "0111", "name": "莘庄" },
  { "code": "0112", "name": "外环路" },
  { "code": "0113", "name": "莲花路" },
  { "code": "0114", "name": "锦江乐园" },
  { "code": "0115", "name": "上海南站" }
]
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| code | string | 站点代码，前两位是线路号，后两位是站内序号 |
| name | string | 站点名称 |

**站点代码示例对照：**

| 代码 | 线路 | 示例站点 |
|------|------|---------|
| 01xx | 1号线 | 0111 莘庄 |
| 02xx | 2号线 | 0235 虹桥火车站 |
| 03xx | 3号线 | 0329 江湾镇 |
| 04xx | 4号线 | 0401 上海体育馆 |
| 05xx | 5号线 | 0501 莘庄 |
| 41xx | 浦江线 | 4101 沈杜公路 |
| 51xx | 市域机场线 | 5131 虹桥2号航站楼 |

---

## 二、站点坐标接口

### `GET /stations-coords.json`

返回各站点在官方 SVG 底图上的坐标（用于在官方线路图上叠加票价圆圈）。

**请求示例：**
```
GET http://localhost:3000/stations-coords.json
```

**返回结果（截取前几条）：**
```json
[
  { "code": "0111", "name": "莘庄", "x": 845, "y": 2308 },
  { "code": "0112", "name": "外环路", "x": 876, "y": 2251 },
  { "code": "0113", "name": "莲花路", "x": 909, "y": 2207 },
  { "code": "0114", "name": "锦江乐园", "x": 942, "y": 2164 },
  { "code": "0115", "name": "上海南站", "x": 985, "y": 2113 }
]
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| code | string | 站点代码 |
| name | string | 站点名称 |
| x | number | SVG 横坐标 |
| y | number | SVG 纵坐标 |

**坐标说明：**
- 基于官方 SVG 底图 viewBox `0 0 2638.57 3692.52`
- 换乘站多个代码共享同一坐标点

**示例（换乘站多代码共站）：**

| 站点 | 代码列表 | 坐标 |
|------|---------|------|
| 人民广场 | 0123, 0245, 0835 | (1382, 1458) |
| 上海火车站 | 0126, 0323, 0410 | (1323, 1208) |
| 世纪大道 | 0249, 0417, 0632, 0942 | (1666, 1693) |

---

## 三、两站间票价查询

### `GET /api/price?src={起始站代码}&dst={目的站代码}`

查询任意两个站点之间的票价。

**请求参数：**

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| src | string | 是 | 起始站代码 | `0329` |
| dst | string | 是 | 目的站代码 | `1521` |

**请求示例：**
```
GET http://localhost:3000/api/price?src=0329&dst=1521
```

**返回结果：**
```json
{
  "src": "0329",
  "dst": "1521",
  "srcName": "江湾镇",
  "dstName": "紫竹高新区",
  "price": "7"
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| src | string | 起始站代码 |
| dst | string | 目的站代码 |
| srcName | string | 起始站名称 |
| dstName | string | 目的站名称 |
| price | string | 票价（单位：元） |

**更多示例：**
```
# 查到市区站点
GET http://localhost:3000/api/price?src=0235&dst=0101
# 结果: {"src":"0235","dst":"0101","srcName":"虹桥火车站","dstName":"莘庄","price":"7"}

# 相邻站点
GET http://localhost:3000/api/price?src=0329&dst=0328
# 结果: {"src":"0329","dst":"0328","srcName":"江湾镇","dstName":"大柏树","price":"3"}
```

> 注意：如果两个站点间查询失败（罕见），会返回 HTTP 502 和 `{"error":"获取票价失败"}`。

---

## 四、一站到全网票价查询（核心接口）

### `GET /api/prices/{站点代码}`

查询某一站到地铁网络**所有其他站点**的票价。

**请求参数：**

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| origin | string（路径参数） | 是 | 起始站代码 | `0329` |

**请求示例：**
```
GET http://localhost:3000/api/prices/0329
```

**返回结果（截取部分）：**
```json
{
  "origin": "0329",
  "originName": "江湾镇",
  "prices": {
    "0111": "5",
    "0112": "5",
    "0113": "5",
    "0114": "5",
    "0115": "4",
    "0116": "4",
    "0117": "4",
    "0201": "5",
    "1521": "7",        // 江湾镇 → 紫竹高新区 = 7元
    "1629": "11",       // 江湾镇 → 惠南 = 11元
    "0329": "0"         // 到自身 = 0元
  }
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| origin | string | 起始站代码 |
| originName | string | 起始站名称 |
| prices | object | 键 = 目的站代码，值 = 票价（元） |

**prices 对象键值说明：**
- key：目的站代码
- value：到该站的票价（单位：元）
- 起始站自身为 `"0"`
- 缺少某个站点说明该次查询未成功，重新请求会自动补齐

**性能说明：**
- 首次查询某站：约 1-2 分钟（需逐一抓取 ~500 个站点票价）
- 再次查询同一站：秒开（命中缓存）

---

## 五、地铁线路图底图

### `GET /images/linesh.svg`

返回官方上海地铁线路图 SVG 文件（作为底图使用）。

**请求示例：**
```
GET http://localhost:3000/images/linesh.svg
```

**信息：**
- 文件大小：约 1.5 MB
- viewBox：`0 0 2638.57 3692.52`
- 来源：上海地铁官网交互式地图

---

## 六、前端页面

### `GET /`

交互式票价查询界面。

**功能：**
1. 顶部下拉框选择起始站
2. 地图上每个站点显示白色圆圈 + 内部票价数字
3. 起点为红色圆圈 + 白色"起"字
4. 滚轮缩放地图
5. 鼠标拖拽平移地图
6. 点击任意站点圆圈切换起点

**使用截图说明：**
```
┌──────────────────────────────────────────────┐
│ [上海地铁票价速查]  起始站:[江湾镇▾]          │
├──────────────────────────────────────────────┤
│                                              │
│        (官 方 地 铁 线 路 图)                │
│                                              │
│    ⑩   ③   (5)      (7)                     │
│   ___●___●____●____●____●___                 │
│           (4)  (5)   (6)                     │
│                                                │
│          [滚轮缩放 · 拖拽平移 · 点击切换]     │
└──────────────────────────────────────────────┘
[江湾镇 → 全网 530 站，最高 12 元]
```

---

## 附：本地票价存储（重点）

票价数据**永久保存在本地**，不走官网缓存过期。本地有就直接返回，不请求官网。

### 数据文件位置

| 文件 | 路径 | 说明 |
|------|------|------|
| **票价存储** | `data/fares.json` | 永久本地票价库（所有查过的票价对） |
| 票价元数据 | `data/fares.meta.json` | 构建时间、票价对数等统计 |
| 站点列表 | `data/stations.json` | 全部站点代码和名称 |
| 站点坐标 | `data/officialStations.json` | SVG 坐标（副本在 public/stations-coords.json） |
| 官方地图数据 | `data/metromap.json` | 从官网 API 抓取的原始数据 |

### 票价库机制

- **永久保存，无过期时间**。本地命中直接返回，不再请求官网。
- 键格式：`{代码A}:{代码B}`（自动排序，0111:0011 与 0011:0111 是同一键，票价双向对称）
- 当前已有 136315 对票价数据
- 覆盖站点越多，后续查询越接近纯本地秒查

### 查询票价库状态

```
GET /api/fare-stats
```

**返回示例：**
```json
{
  "builtAt": null,
  "stationCount": 0,
  "pairCount": 0,
  "totalPairs": 7274
}
```

- `totalPairs`：已永久保存的票价对数

---

## 附：官网数据源接口（本项目实际调用的上游接口）

本项目后端从上海地铁官网抓取真实票价。官网提供两个票价接口，本项目按策略使用：

### 上游接口 A：简单票价接口

```
GET https://service.shmetro.com/i/p?o={起点代码}&d={终点代码}
```

**响应示例：**
```json
{"flag":0,"data":{"p":"7"}}
```

- `data.p` 为票价（单位：元）

**限制：**
- 对 18 号线北段新站（1847/1848/1850/1851/1852）返回空 `data:{}`，查不到
- 对 **市域机场线(51xx) 站点返回错误的高价**

### 上游接口 B：行程规划接口（plantrip，官网 cphc 页面实际使用）

```
GET https://m.shmetro.com/interface/plantrip/pt.aspx
    ?func=plantrip
    &startId={起点代码}
    &endId={终点代码}
    &planTime=12:00
    &week=1
    &ticket=oneCard
    &type=1
```

**响应示例（关键字段）：**
```json
{
  "startId": "5133",
  "startStName": "景洪路",
  "endId": "1521",
  "endStName": "紫竹高新区",
  "pathList": [
    {
      "price": "4",
      "stationNum": "7",
      "time": "19",
      "passStationList": [...]
    }
  ]
}
```

- `pathList[0].price` 为票价（单位：元）
- 官网 `https://service.shmetro.com/cphc/index.htm` 票价页面实际使用该接口，因此价格最准确

### 本项目选择策略

| 条件 | 使用的接口 |
|------|-----------|
| 普通站点 | 简单接口 `/i/p`（快）；失败再试行程接口 |
| 涉及 18 号线新站 | 行程接口（简单接口查不到） |
| 涉及市域机场线(51xx) | 行程接口（简单接口价格错误） |
| 长江西路(1848) 做起点 | 行程接口反向查询（票价对称） |

> **为什么用行程接口**：市域机场线站点在简单接口 `/i/p` 中的价格严重偏高（如景洪路→莘庄报 12 元，实际 4 元），而官网页面用行程接口返回正确价格。因此涉及 51xx 站一律走行程接口。

---

## 关联命令

```bash
npm start        # 启动服务（端口 3000）
npm run warmup   # 预计算完整票价矩阵，永久存入 data/fares.json（跑完所有站点纯本地秒查）
npm run generate # 重新生成旧版 GIS 地图数据（不常用）
npm run rebuild  # 从官方 API 重新提取站点坐标
```

> **提示**：第一次查某个站仍需请求官网（约 1-2 分钟），之后永久本地秒查。跑一次 `npm run warmup` 可一次性构建全网票价矩阵，之后所有站点都无需联网。